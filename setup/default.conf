# OPENVPN_RESTART_CMD="systemctl restart openvpn@server.service"
# OPENVPN_SERVER_CONF=/etc/openvpn/server.conf
# OVPN_LISTEN_HOST=10.42.42.10  ## make it configurable with interface wildcard through GUI
# OVPN_LISTEN_PORT=8042
EASYRSA_PATH=/usr/share/easy-rsa/
ADMIN_ACCOUNT=/etc/openvpn/admin-config.json
